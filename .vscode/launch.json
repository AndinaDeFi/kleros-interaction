{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Truffle Test (debuggable)",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/node_modules/truffle/build/cli.bundled.js",
      "args": [
        "test",
        "test/escrowWithDisputes.js",
        "--network",
        "test",
        "--compile-none"
      ]
    }
  ]
}
